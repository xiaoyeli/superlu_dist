		SuperLU_DIST TESTING
		====================

This directory contains testing programs to test various functions
provded in SuperLU_DIST. 

1. To run the real test (pdtest), you may type:
  % mpiexec -n <np> pdtest -r <process row> -c <process columns> -f g20.rua 


